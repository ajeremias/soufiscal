<%= form_with(model: complaint_address, local: true) do |form| %>
  <% if complaint_address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(complaint_address.errors.count, "error") %> prohibited this complaint_address from being saved:</h2>

      <ul>
      <% complaint_address.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :complaint_address_nome %>
  </div>

  <div class="field">
    <%= form.label :state_id %>
    <%= form.text_field :state_id, id: :complaint_address_state_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
